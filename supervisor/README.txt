This was in:

/etc/supervisor/

There is a missing .conf file from ./conf.d/; there should be a config file for the Efoy logger script in there. Not sure how it got left out.

See supervisord install instructions for proper use of supervisord.
